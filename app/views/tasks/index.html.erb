<h1>タスク一覧画面</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %>: <%= task.status %> &gt; <%= task.content %></li>
  <% end %>
</ul>

<%= link_to 'タスクを追加する', new_task_path %>
